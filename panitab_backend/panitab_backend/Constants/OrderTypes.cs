namespace panitab_backend.Constants
{
    public class OrderTypes
    {
        public const string CREDIT = nameof(CREDIT);    // Venta a crédito
        public const string CASH = nameof(CASH);    // Venta en efectivo
        public const string EMPLOYEE = nameof(EMPLOYEE);   // Venta a empleados
        public const string RETAIL = nameof(RETAIL); // Venta a particulares
    }
}
